import React from 'react'

const SignOut = () => {
        return(
            <a
                className="nav-item"
                onClick={this.props.signOut}
            >
                Sign Out
            </a>
        )           
}

export default SignOut